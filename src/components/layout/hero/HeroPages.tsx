
import { useTranslations } from "next-intl";

export default function HeroPages() {
    const t = useTranslations("heroPages");
    return (

        <section className="hero-pages-section">
            <div className="container mx-auto h-[50vh]">
               hero
            </div>
        </section>

    )
}